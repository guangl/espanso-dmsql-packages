matches:
  - trigger: :psp
    replace: "cat $ss|awk 'BEGIN { s=\"\"; }\n/^Thread/ { print s;s=\"\"; }\n/^#/ { if (s!=\"\"){ s=s\",\"$4} else { s=$4} }\nEND {print s}\n' | sort | uniq -c |sort -r -n -k 1,1"
